sourceSets.main.java.srcDirs = [ "src/" ]

sourceCompatibility = '1.7'
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

ext {
	mainClassName = "com.freebandz.lost_in_peril.IOSLauncher"
}
/*
//   <pattern>de.tomgrill.gdxfacebook.ios.IOSFacebookLoader</pattern>

    <framework>FBSDKCoreKit</framework>
    <framework>FBSDKShareKit</framework>
    <framework>FBSDKLoginKit</framework>

robovm.xml
 */
launchIPhoneSimulator.dependsOn build
launchIPadSimulator.dependsOn build
launchIOSDevice.dependsOn build
createIPA.dependsOn build

robovm {
	archs = "thumbv7:arm64"
    //iosSignIdentity = "Apple Development: Billy Gretty (JPL5X36FFC)"
    iosSignIdentity = "Apple Distribution: Billy Gretty (U9XL4P644C)"
    //iosProvisioningProfile = "Apple Distribution: Billy Gretty (U9XL4P644C)"
    //iosProvisioningProfile = "iOS Team Provisioning Profile: com.freebandz.Lost-in-Peril"
    iosProvisioningProfile = "Distributing Provisioning Profile"
    iosSkipSigning = false
    stdoutFifo = ""
    stderrFifo = ""
}

eclipse.project {
    name = appName + "-ios"
    natures 'org.robovm.eclipse.RoboVMNature'
}